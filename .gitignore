# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Convex
.convex/
# convex/_generated is committed (Convex-recommended) so clients import the typed
# `api` and the repo typechecks without running codegen first. Regenerate with
# `bunx convex dev`; never edit generated files by hand.

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Test artifacts
coverage/
test-output/
